plugins {
    id 'java'
    id 'com.gradleup.shadow' version '9.3.1' apply false
}

subprojects {
    group = 'dev.kyriji'
    version = ''

    java {
        toolchain {
            languageVersion.set(JavaLanguageVersion.of(25))
        }
    }
}