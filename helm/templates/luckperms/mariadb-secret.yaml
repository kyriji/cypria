{{- if .Values.luckperms.enabled }}
{{- $mariadbPassword := randAlphaNum 32 }}
apiVersion: v1
kind: Secret
metadata:
  name: mariadb-secret
type: Opaque
data:
  MYSQL_ROOT_PASSWORD: {{ $mariadbPassword | b64enc | quote }}
stringData:
  MARIADB_URI: "mysql://root:{{ $mariadbPassword }}@mariadb-service:3306/luckperms"
{{- end }}
